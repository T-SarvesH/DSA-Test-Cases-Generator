{
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.6.1"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.php" }
  ],
  "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npm run build && mkdir -p storage/logs bootstrap/cache && chmod -R 777 storage bootstrap/cache"
}
